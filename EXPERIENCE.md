# 存档修改核心经验（血泪踩坑提炼）

> 写给以后要改这游戏（或任何 Unity + Json.NET 单机存档）的人。
> 全部结论都经过**实机 + 运行内存**验证，不是网上瞎猜。网上攻略关于格式的部分基本是错的。

---

## 0. 环境事实（先记死）

- 游戏：侠客风云传前传（Tale of Wuxia: The Pre-Sequel），Steam appid `650760`
- **游戏内部版本 = `1.0.2.2`**（看 `YoungHero_Data/output_log.txt`，不是 exe 的 FileVersion）
- 引擎 Unity 4.7.2f1 + Mono，命名空间 `Heluo.Wulin`
- **真实存档目录**：`D:\steam_jie\userdata\1187465257\650760\remote\`
  - 注意 `AppData\Roaming\SmartSteamEmu\...` 是旧破解版存档，**已过时，别碰**
- 存档文件：`SaveN.Save` + 索引 `SaveTitle.SaveTitle`（JSON 数组，第 i 项 ↔ `Save{i}.Save`）
- 配置表（`.pk`）+ `Assembly-CSharp.dll` 在磁盘上是**加密**的；**解密后明文只在进程运行时内存里**

---

## 1. 存档格式铁律：绝对不能有 BOM（最致命）

- 游戏读档是 `bytes → Encoding.UTF8.GetString() → Json.NET`，**不走 `File.ReadAllText`**
- 所以 BOM（`EF BB BF`）不会被自动剥离，首字符变成 `U+FEFF` → 直接 `JsonReaderException: Unexpected character ... Path '', line 0, position 0` → **坏档、进不去**
- **正确做法**：`open(path,'rb').read()` → `decode('utf-8')`（不是 utf-8-sig）→ 改 → `encode('utf-8')` 写回，**绝不带 BOM**
- 排查坏档第一动作：看 `output_log.txt` 的 `JsonReaderException`，并 `head` 前 4 字节对比（正常应为 `7b 22 6d 5f` = `{"m_`）
- **存档字节数必须与原档尽量一致**——多 3 字节往往就是误加的 BOM

---

## 2. 写回策略：字节级最小替换，别整体重序列化

- 整体 `json.dumps` 重序列化风险大：浮点格式、键序、分隔符、空白都可能变，游戏虽用 Json.NET 容错，但宁可不动
- **首选**：以原档为基准读 bytes → 局部正则/字符串替换 → 写回
- **次选（现在更常用，更稳）**：`json.loads` 整体 → 改 dict → `json.dumps(separators=(',',':'))` 紧凑回写 + **强校验脚本**确认「除目标外全部不变」
- 不论哪种，**写完都断言**：首 3 字节 ≠ `EF BB BF`，且 `json.loads` 能过

### 角色技能列表的真实结构（早期误判过的坑）

- `NeigongList`（内功）/ `RoutineList`（武功/套路）不是 `[id,lvl]` 元组数组，而是**对象数组**：
  ```json
  {"iLevel":10,"m_iAccumulationExp":30000,"bUse":false,"iSkillID":70069}
  ```
- 移除要用对象级操作（列表推导按 `iSkillID` 过滤，或正则 `r',\s*\{[^{}]*"iSkillID":70069[^{}]*\}'`）
- `_MartialArts` 只是兵器使用次数统计，**不是招式列表**，别被名字骗了

### 背包物品 schema

```json
{"m_bNew":true,"m_iAmount":1,"m_ItemID":121034}
```
（紧凑、小写 `true/false`）

### 队伍增减

- 队伍成员**只**由顶层 `m_TeammateList`（int 数组）决定，NPC 自身无 `bInTeam` 字段
- 增删直接改这个数组；本游戏队伍上限 = **9 人**
- 加人前确认该 `iNpcID` 在 `m_NpcList` 里是 `NpcType=2` 的合法同伴，否则可能因缺数据崩

---

## 3. 配置表加密：静态打不开，靠运行时内存 UTF-8 抠

- `.pk` 是加密 Unity AssetBundle（magic `b758af21c8939901`，熵≈7.9），静态 XOR/deflate/brotli/lz4/zstd 全失败 → **静态无解**
- **突破口**：解密后的 TextAsset 原文以 **UTF-8 明文常驻进程内存**（注意是 UTF-8，不是 UTF-16LE！早期用 UTF-16LE 扫描全部 0 命中，换成 UTF-8 才成功）
- 抠法：`ctypes.OpenProcess(0x0410)` + `VirtualQueryEx` + `ReadProcessMemory` 遍历 64 位进程全部 COMMIT 可读区，搜 UTF-8 关键字，定位表头后读到 `\x00` 即为完整表
- 同样能抠出解密后的 `Assembly-CSharp.dll`
- **关键教训**：静态搜不到字符串 ≠ 功能不存在（DLL 无 MZ 头、靠魔改 `mono.dll` 运行时解密）。判断"有没有某个物品/秘籍"必须以**运行时内存**为准，别凭静态推理下结论

### 内存扫描实战技巧

- 多张表（SkillData / ItemData / 事件脚本 / NpcData）常混在同一块 4MB 内存区
- 行分隔 `\r\n`，字段分隔 `\t`（不是空格！网上攻略写空格是错的）
- 用已知锚点定位：如秘籍锚 `121034\t`，角色锚 `N1节点 0 <说话人ID> 1 0 0 0 <台词>`

---

## 4. 已知数据映射（内存实证，复用）

### 角色 ID（内存实证，别信"编号相邻"）

| 角色 | iNpcID | 实证来源 |
|------|--------|---------|
| 卫紫绫 | 200000 | 首轮内存 NPCData |
| 蓝婷 | 100058 | 对话内存 `... 100058 ... 蓝婷` |
| 史燕 | 100059 | TalentList MOD 挂载对象 |
| 水盼盼 | 200016 | 对话自称"盼盼" + 冰心剑典招牌；存档印证 |
| 荆棘 | 210002 | NeigongList 含 60036 血刀经 |
| 龙墨 | 200017 | NeigongList 含 70069 正气决 |
| 谷月轩 | 210001 | RoutineList 含 100010 + 140087 |

> ⚠️ 同伴 ID 不连续（100057 在所有存档从不出现=空槽）；"对练列表里的 100058"是对手蓝婷，不是水盼盼——这是加错人的经典坑。

### 技能 → 秘籍物品（ItemData type-6 行，`col14` = 所教技能 ID）

| 技能 | 技能ID | 秘籍物品ID | 备注 |
|------|--------|-----------|------|
| 血刀经 | 60036 | 121010 | 内功秘籍 |
| 正气决 | 70069 | 121034 | 内功秘籍 |
| 苗蛊毒功 | 60074 | 121043 | 内功秘籍（残本） |
| 水浒英雄掌 | 100010 | 100010 | **type-4 套路物品本身**，可作"秘籍"放回背包 |
| 林冲策马鞭 | 140087 | **无** | 水浒英雄掌套路里由事件(100165)直接授予的招式，**游戏里没有对应物品 ID**，无法以秘籍形式放回背包 |

> 找某技能的秘籍 = 找 ItemData 中 `field[2]=='6'`（秘籍类型）且 `field[14]==技能ID` 的物品。
> 全内存扫描确认：0 个 type-6 秘籍教 100010/140087；140087 整个 ItemData 无物品行。

---

## 5. 退还武功/内功为秘籍：标准流程（refund_skill.py）

1. 备份（`_save_backups/` 或 `.bak_时间戳`，**别留在 `remote\` 里**，会被 Steam Cloud 同步、目录不干净）
2. 确认目标角色 `iNpcID` 与技能 `iSkillID` 确在对应 `NeigongList`/`RoutineList`
3. 查 skill→book 映射（见上表）；无对应秘籍则 `book=None` 只移除
4. 移除技能对象 + 背包 append 秘籍（数量 1）
5. UTF-8 无 BOM 写回 + 强校验（除目标外所有角色技能列表、背包原条目零改动）

### 已实测案例

- **Save16 卫紫绫**：移除 70069(正气决)+60074(苗蛊毒功)，背包 +121034 +121043
- **Save19 荆棘/龙墨/谷月轩**：移除 60036 / 70069 / 100010+140087，背包 +121010 +121034 +100010（140087 仅移除）

---

## 6. 生效须知（每次都要提醒）

- 改的是**落盘存档**，游戏读的是内存
- 若游戏进程在运行且自动存档（或你手动存），会把没改的内存版写回磁盘、**覆盖本次修改**
- 必须 **退出游戏 → 重新启动 → 读目标存档** 才看得到变化
- 建议顺序：先退出游戏 → 确认存档已是改后版本 → 再读档

---

## 7. 调试方法论（可复用套路）

- `output_log.txt` 是 Unity 日志金矿：MOD 加载打印 `Mod active`，解析失败回显失败行，**没被回显的行即正确格式**
- NTFS `DisableLastAccess=2` 时最后访问时间启用 → 可用"记录 atime → 启动游戏 → 比对 atime"精确判定游戏究竟读了哪个路径
- 多变体探针：一次放多行不同格式，报错日志回显失败行，没回显的即正确，一次启动测完
- `.pk` 是 AssetBundle，内存可见 `CAB-xxxx` 与 TextAsset 名；Mono 字符串前缀是 4 字节 vtable + 4 字节 sync + 4 字节长度

---

## 8. MOD 文件格式（顺带记录，曾踩坑）

- 生效路径唯一：`<游戏根>/Mods/<版本>/Config/TextFiles/TalentNewData.txt`
- **UTF-16 LE + BOM**（`FF FE` 开头）；字段 **TAB** 分隔；角色天赋是**整表替换**不是按 ID 覆盖（只写 2 行会导致其他天赋全没）
- 天赋 305 妙手空空 = `104 70 70`；339 神偷盗帅 = `104 80 80`（类型 104=偷取，两数值=基础/残血几率）
- 新增天赋 ID 可超出原范围（如 354），全局可见但无人持有则其他存档无感，实现"单点生效"
